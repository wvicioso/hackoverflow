<!DOCTYPE html>
<html lang="en">
<head>
  <title></title>
</head>

<body>
  <div class="nav-div">
  <nav>
      <ul class="nav-ul">
      <li class="nav-li"><a class="nav-a" href="/">Home</a></li>
      <li class="nav-li"><a class="nav-a" href="/users/<%= session[:id]%>">Profile</a></li>
      <li class="nav-li"><a class="nav-a" href="/questions">Questions</a></li>
      <% if session[:id] %>
        <span>Welcome <%= User.find(session[:id]).username %></span>
          <a href="/sessions/logout">logout</a>
          <a href="/user/<%=session[:id]%>/edit">edit</a>
      <% else %>
          <li class="nav-li"><a class="nav-a" href="/users/new">Create</a></li>
          <form class="new_user" action="/sessions" method="post">
            <input type="text" name=user[username] placeholder="username">
            <input type="text" name=user[password] placeholder="password">
            <input type="submit" value="login">
          </form>
      <% end %>
    </ul>
  </nav>
</div>
  <%= yield %>
</body>
</html>
